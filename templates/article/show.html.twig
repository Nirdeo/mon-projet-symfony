{% extends 'base.html.twig' %}

{% block body %}
    <h1>Article {{ article.title }}</h1>
    <p>{{ article.content }}</p>
    <p><a href="{{ path('articles') }}">Retour à la liste</a></p>
{% endblock %}